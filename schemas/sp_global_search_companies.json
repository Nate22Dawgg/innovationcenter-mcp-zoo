{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "S&P Global Search Companies Input",
  "description": "Input schema for searching companies using S&P Capital IQ",
  "type": "object",
  "properties": {
    "query": {
      "type": "string",
      "description": "Company name, ticker symbol, or CIQ identifier to search for",
      "examples": ["Apple Inc.", "AAPL", "CIQ0000000000000000"]
    },
    "country": {
      "type": "string",
      "description": "Filter by country code (ISO 3166-1 alpha-2, e.g., 'US', 'GB')",
      "pattern": "^[A-Z]{2}$",
      "examples": ["US", "GB", "JP", "DE"]
    },
    "sector": {
      "type": "string",
      "description": "Filter by industry sector",
      "examples": ["Technology", "Healthcare", "Financial Services", "Energy", "Consumer Discretionary"]
    },
    "limit": {
      "type": "integer",
      "minimum": 1,
      "maximum": 100,
      "default": 20,
      "description": "Maximum number of companies to return"
    }
  },
  "required": ["query"],
  "additionalProperties": false
}

