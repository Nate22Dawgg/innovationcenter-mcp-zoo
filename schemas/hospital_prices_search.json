{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Hospital Prices Search Input",
  "description": "Input schema for searching hospital pricing information",
  "type": "object",
  "properties": {
    "cpt_code": {
      "type": "string",
      "description": "CPT or HCPCS procedure code",
      "examples": ["99213", "27447", "45378"]
    },
    "location": {
      "type": "string",
      "description": "Location string (city, state or zip code)",
      "examples": ["New York, NY", "90210", "San Francisco, CA"]
    },
    "radius": {
      "type": "integer",
      "minimum": 1,
      "maximum": 100,
      "default": 25,
      "description": "Search radius in miles"
    },
    "zip_code": {
      "type": "string",
      "pattern": "^[0-9]{5}(-[0-9]{4})?$",
      "description": "ZIP code for location-based search"
    },
    "state": {
      "type": "string",
      "pattern": "^[A-Z]{2}$",
      "description": "US state code (two letters)"
    },
    "limit": {
      "type": "integer",
      "minimum": 1,
      "maximum": 200,
      "default": 50,
      "description": "Maximum number of results to return"
    }
  },
  "required": ["cpt_code"],
  "additionalProperties": false
}

