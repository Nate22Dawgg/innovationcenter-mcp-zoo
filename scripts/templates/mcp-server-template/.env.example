# Template MCP Server Environment Variables
#
# Copy this file to .env and fill in your actual values.
# When creating a new MCP server from this template, update these variable names
# to match your server's configuration.

# Base URL for the upstream API
TEMPLATE_BASE_URL=https://api.example.com

# API key for authentication
TEMPLATE_API_KEY=your-api-key-here

# Configuration validation behavior
# Set to "true" to fail-fast on config errors (default)
# Set to "false" to allow server to start but disable tools (fail-soft)
TEMPLATE_FAIL_FAST=true
